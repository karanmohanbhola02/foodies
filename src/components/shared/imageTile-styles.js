import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    imageContainer: {
        width: 110,
        height: '100%',
        borderRadius: 8,
        overflow: 'hidden'
    },
    image: {
        width: '100%',
        height: '100%'
    }
});

export default styles;